{
  "name": "envo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "envo": "bun run packages/cli/src/index.ts",
    "typecheck": "bun run --filter '*' typecheck",
    "test": "cd packages/core && bun test"
  },
  "packageManager": "bun@1.2.0",
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^5.8.3"
  }
}